<%= form_for(@wedding_request) do |f| %>
  <% if @wedding_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wedding_request.errors.count, "error") %> prohibited this wedding_request from being saved:</h2>

      <ul>
      <% @wedding_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sender_id %><br>
    <%= f.number_field :sender_id %>
  </div>
  <div class="field">
    <%= f.label :reciever_id %><br>
    <%= f.number_field :reciever_id %>
  </div>
  <div class="field">
    <%= f.label :is_accepted %><br>
    <%= f.check_box :is_accepted %>
  </div>
  <div class="field">
    <%= f.label :is_cancel %><br>
    <%= f.check_box :is_cancel %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
